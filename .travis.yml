language: python

notifications:
  email: chowmichedu@gmail.com

virtualenv:
  system_site_packages: true

python:
  - "2.7"

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install -qq libhdf5-serial-dev

install:
    - pip install -r requirements.txt

script: "nosetests"
